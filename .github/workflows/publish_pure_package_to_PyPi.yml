name: Build and Upload Wheel

on:
  workflow_call:
    inputs:
      python-version:
        required: true
        type: string
      test-command:
        description: Optional command to run after installing the wheel in a fresh environment.
        required: false
        type: string
        default: ""
    secrets:
      PYPI_API_TOKEN:
        required: true


jobs:
    ManyLinux_x86_64:
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v5
          with:
            fetch-depth: 0
            submodules: 'true'

        - uses: actions/setup-python@v6
          with:
              python-version: ${{ inputs.python-version }}

        - name: "Running unittests and building wheel"
          shell: bash
          run: |
              python -m pip install --upgrade pip
              python -m pip install wheel build
              python -m pip install -e .[testing]
              pytest -s
              python -m build

        - name: Test built wheel in isolation
          if: inputs.test-command != ''
          shell: bash
          run: |
              set -Eeuo pipefail
              test_directory="$(mktemp -d)"
              python -m venv "${test_directory}/venv"
              "${test_directory}/venv/bin/python" -m pip install dist/*.whl
              cd "${test_directory}"
              PATH="${test_directory}/venv/bin:${PATH}" bash -c "${{ inputs.test-command }}"

        - name: "Upload: Wheel Artifact"
          uses: actions/upload-artifact@v6
          with:
              name: wheel-artifact
              path: ./dist/*

    publish_wheel:
        if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
        needs: [ManyLinux_x86_64]
        runs-on: ubuntu-latest
        steps:
          - uses: actions/checkout@v5
            with:
                fetch-depth: 0
                submodules: 'true'

          - name: "Set-up: Python ${{ inputs.python-version }}"
            uses: actions/setup-python@v6
            with:
                python-version: "${{ inputs.python-version }}"

          - name: "Install: Dependencies"
            shell: bash
            run: |
                python -m pip install --upgrade pip
                python -m pip install twine

          - name: "Download: Wheel Artifact"
            uses: actions/download-artifact@v5
            with:
                name: wheel-artifact
                path: ./dist

          - name: "Publish: Wheel"
            uses: pypa/gh-action-pypi-publish@v1.5.1
            env:
                MY_SECRET: ${{ inputs.password }}
            with:
                packages_dir: ./dist
                user: __token__
                password: "${{ secrets.PYPI_API_TOKEN }}"
                verbose: true
